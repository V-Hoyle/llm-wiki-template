# Cursor API key for background ingest + weekly lint workers.
# Get a key from: Cursor Settings → API (or cursor.com dashboard)
# CURSOR_API_KEY=your_key_here

# Optional: private git remote for daily wiki backup (push after local commit).
# Example: git@github.com:youruser/llm-wiki-private.git
# WIKI_GIT_REMOTE=
